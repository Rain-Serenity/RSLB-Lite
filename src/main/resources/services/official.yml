# 正版登录服务的唯一 ID，取值范围 0-127，不可与其他服务重复。
id: 0

# 登录服务显示名称。
name: '正版登录'

# 初始档案名格式，{name} 会被替换为外置登录用户名，空格自动转换为 '_'。
# 默认值: '{name}'
initNameFormat: '{name}'

# 初始游戏内 UUID 生成方式：
#   DEFAULT - 直接使用外置登录服务返回的 UUID
#   OFFLINE - 按原版离线模式算法（OfflinePlayer:名称）生成，可获得固定 UUID
#   RANDOM  - 每次登录随机生成 UUID
# 默认值: DEFAULT
initUUID: DEFAULT

# 固定为 OFFICIAL，表示 Mojang/Microsoft 正版验证。
serviceType: OFFICIAL

# 皮肤修复（Skin Restorer）设置：
#   restorer - 修复时机: OFF 关闭 / LOGIN 登录时修复
#   method   - 皮肤获取方式: URL 直接链接 / UPLOAD 上传给服务器
#   timeout / retry / retryDelay - 皮肤请求网络参数
#   mineskinApiKey - MineSkin API Key，留空使用匿名额度（额度低），推荐到 https://account.mineskin.org 申请后填写
#   visibility    - 生成的皮肤在 MineSkin 图库中的可见性
#   proxy    - 皮肤请求使用的代理
# 默认值: OFF 关闭
skinRestorer:
  # 皮肤修复时机: OFF / LOGIN
  # 说明: 外置登录服务的皮肤纹理由第三方私钥签名，正版客户端无法识别而不显示；
  # 开启后 RSLB 在登录时将该纹理交给 MineSkin 生成正版签名的纹理，使皮肤对正版客户端可见。
  # 正版登录建议保持OFF
  # 默认值: 'OFF'
  restorer: 'OFF'

  # 皮肤获取方式: URL / UPLOAD
  # URL - 将皮肤图片链接提交给 MineSkin，由 MineSkin 服务器下载；
  # UPLOAD - 由 RSLB 下载皮肤图片后上传给 MineSkin，服务器无法直接访问皮肤站时使用。
  # 默认值: URL
  method: URL

  # 皮肤请求超时时间，单位毫秒。
  # 默认值: 10000
  timeout: 10000

  # 皮肤请求失败后的重试次数。
  # 默认值: 2
  retry: 2

  # 每次重试前的等待时间，单位毫秒。
  # 默认值: 5000
  retryDelay: 5000

  # MineSkin API Key（支持 Bearer 认证），留空使用匿名额度。
  # 获取地址: https://account.mineskin.org
  # 默认值: ''
  mineskinApiKey: ''

  # 生成的皮肤在 MineSkin 图库中的可见性: PUBLIC / UNLISTED / PRIVATE
  # 默认值: PUBLIC
  visibility: PUBLIC

  # 皮肤请求代理设置。
  proxy:
    # 代理类型: DIRECT / HTTP / SOCKS
    # 默认值: DIRECT
    type: DIRECT

    # 代理地址。
    # 默认值: '127.0.0.1'
    hostname: '127.0.0.1'

    # 代理端口。
    # 默认值: 1080
    port: 1080

    # 代理认证用户名，留空表示无需认证。
    # 默认值: ''
    username: ''

    # 代理认证密码，留空表示无需认证。
    # 默认值: ''
    password: ''

# 正版 Yggdrasil 会话验证设置。
yggdrasilAuth:
  # 会话验证请求代理设置（服务器无法直连会话服务器时使用，如网络受限环境）。
  authProxy:
    # 代理类型: DIRECT / HTTP / SOCKS
    # 默认值: DIRECT
    type: DIRECT

    # 代理地址。
    # 默认值: '127.0.0.1'
    hostname: '127.0.0.1'

    # 代理端口。
    # 默认值: 1080
    port: 1080

    # 代理认证用户名，留空表示无需认证。
    # 默认值: ''
    username: ''

    # 代理认证密码，留空表示无需认证。
    # 默认值: ''
    password: ''

  official:
    # Mojang 官方会话服务器地址。
    sessionServer: 'https://sessionserver.mojang.com'

  # 网络失败后的重试次数，取值建议 0-3。
  retry: 1

  # 每次重试前的等待时间，单位毫秒。
  retryDelay: 300

  # 网络请求超时时间，单位毫秒，建议 3000-30000。
  timeout: 10000

  # 是否向会话服务器提交玩家 IP；通常保持 false。
  trackIp: false
